import "server-only";
import type { OfficeTemplate } from "@/modules/office/shared/template-registry";

/** slides editor templates. Each build() returns this editor's content model. */
export const SLIDES_TEMPLATES: OfficeTemplate[] = [];
