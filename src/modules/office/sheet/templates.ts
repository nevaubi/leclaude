import "server-only";
import type { OfficeTemplate } from "@/modules/office/shared/template-registry";

/** sheet editor templates. Each build() returns this editor's content model. */
export const SHEET_TEMPLATES: OfficeTemplate[] = [];
