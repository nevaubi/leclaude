import "server-only";
import type { OfficeTemplate } from "@/modules/office/shared/template-registry";

/** pdf editor templates. Each build() returns this editor's content model. */
export const PDF_TEMPLATES: OfficeTemplate[] = [];
