import "server-only";
import type { OfficeTemplate } from "@/modules/office/shared/template-registry";

/** word editor templates. Each build() returns this editor's content model. */
export const WORD_TEMPLATES: OfficeTemplate[] = [];
