import "server-only";
import type { Database } from "@/lib/db";

/** workflows module seed. Populate the module's collections with stable ids (idempotent). */
export function seedWorkflows(db: Database) {
  void db;
}
