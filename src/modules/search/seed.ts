import "server-only";
import type { Database } from "@/lib/db";

/** search module seed. Populate the module's collections with stable ids (idempotent). */
export function seedSearch(db: Database) {
  void db;
}
