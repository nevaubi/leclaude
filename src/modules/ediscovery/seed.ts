import "server-only";
import type { Database } from "@/lib/db";

/** ediscovery module seed. Populate the module's collections with stable ids (idempotent). */
export function seedEdiscovery(db: Database) {
  void db;
}
