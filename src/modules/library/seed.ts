import "server-only";
import type { Database } from "@/lib/db";

/** library module seed. Populate the module's collections with stable ids (idempotent). */
export function seedLibrary(db: Database) {
  void db;
}
